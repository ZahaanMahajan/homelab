apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: nextcloud-zahaan-online
  namespace: nextcloud

spec:
  secretName: nextcloud-zahaan-online-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - nextcloud.zahaan.online
