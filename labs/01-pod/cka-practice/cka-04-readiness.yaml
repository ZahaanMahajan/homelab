apiVersion: v1
kind: Pod

metadata:
  name: cka-readiness

spec:
  containers:
    - name: nginx
      image: nginx:latest

      readinessProbe:
        httpGet:
          path: /
          port: 80

        initialDelaySeconds: 3
        periodSeconds: 5
